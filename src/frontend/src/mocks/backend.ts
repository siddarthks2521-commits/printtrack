import type { backendInterface } from "../backend.d";
import { OrderStatus, OrderType } from "../backend.d";

export const mockBackend: backendInterface = {
  addOrder: async (args) => ({
    id: BigInt(Date.now()),
    customerName: args.customerName,
    status: OrderStatus.Pending,
    createdAt: BigInt(Date.now()),
    dueDate: args.dueDate,
    isPaid: false,
    orderType: args.orderType,
    notes: args.notes,
    quantity: args.quantity,
    phoneNumber: args.phoneNumber,
    price: args.price,
  }),
  addQuotation: async (args) => ({
    id: BigInt(Date.now()),
    customerName: args.customerName,
    createdAt: BigInt(Date.now()),
    orderType: args.orderType,
    quantity: args.quantity,
    unitPrice: args.unitPrice,
    phoneNumber: args.phoneNumber,
    totalPrice: Number(args.quantity) * args.unitPrice,
  }),
  convertQuotationToOrder: async (_quotationId, dueDate, notes) => ({
    id: BigInt(Date.now()),
    customerName: "Converted Customer",
    status: OrderStatus.Pending,
    createdAt: BigInt(Date.now()),
    dueDate,
    isPaid: false,
    orderType: OrderType.Print,
    notes,
    quantity: BigInt(100),
    phoneNumber: "",
    price: 0,
  }),
  deleteOrder: async () => true,
  deleteQuotation: async () => true,
  getDailyRevenue: async (date) => ({
    revenue: 0,
    date,
    ordersCount: BigInt(0),
  }),
  getMonthlyRevenue: async (year, month) => ({
    month,
    revenue: 0,
    year,
    ordersCount: BigInt(0),
  }),
  getOrder: async () => null,
  getOrders: async () => [],
  getOrdersCompletedCount: async () => BigInt(0),
  getPendingPaymentsInfo: async () => ({
    totalUnpaidAmount: 0,
    totalUnpaidOrders: BigInt(0),
  }),
  getQuotations: async () => [],
  updateOrder: async () => true,
  updateOrderStatus: async () => true,
};
